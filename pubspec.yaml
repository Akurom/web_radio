name: web_radio
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=2.19.2 <3.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  basic_utils: ^5.5.2
  google_fonts: ^4.0.3
  shared_preferences: ^2.0.20
  cached_network_image: ^3.2.3
  equatable: ^2.0.5
  flutter_bloc: ^8.1.2
  dio: ^5.0.3
  just_audio: ^0.9.32
  #flutter_radio_player: ^2.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

flutter:

  uses-material-design: true
